{
    "type": "object",
    "properties": {
        "buttons": {
            "title": "Options - buttons",
            "description": "customize a set of buttons to render in Options",
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "caption": { "type": "string" },
                    "url": { "type": "string" }
                }
            }
        },
        "newtab": {
            "title": "Chrome browser new tab URL",
            "description": "set URL on user's request for a new tab",
            "type": "string"
        },
        "UserAgent": {
            "title": "HTTP header User-Agent masking",
            "description": "Override browser identification to the specified domain(s)",
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "domain": { "type": "string" },
                    "value": { "type": "string" }
                }
            }
        },
        "webOMRframePrint": {
            "title": "Inject print action icon into header frame",
            "description": "Allows user's print request to include all pages in the main frame",
            "type": "boolean"
        }
    }
}
